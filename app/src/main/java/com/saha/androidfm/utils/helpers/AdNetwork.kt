package com.saha.androidfm.utils.helpers

/**
 * Enum class representing available ad networks.
 * 
 * Use this enum to switch between different ad network providers.
 * 
 * @property ADMOB Google AdMob ad network
 * @property META Meta (Facebook) Audience Network
 */
enum class AdNetwork {
    ADMOB,
    META
}
